# Огляд

Високорівнева карта: чим я займаюсь, де що лежить, що відкрито.
Джерело: «📌 ПОСТІЙНИЙ БЛОК» MEMORY.md (станом на засів вікі, 2026-07-07).

## Архітектура (гібрид)
Знання живуть у ДВОХ шарах:
- **[[baza-znan|Notion «UNITEX \| База знань»]]** — бізнес-довідка (фрахт, коносаменти, Т1,
  наземна доставка, документи) + щоденні нотатки (ти/бот). Росте поступово.
- **Репо `wiki/`** (цей набір файлів) — методики розрахунків і техдокументація проєктів.

## Активні проєкти
- **[[bot]]** — особистий Telegram-бот на Claude (bot.py, agent_bot.py). Працює на VPS, autodeploy щохвилини.
- **[[finzvit]]** — Оперативний фінансовий звіт з даних 1С Експедитор (Google Sheets + дашборд).
- **[[dyspetcheryzatsiya]]** — міні-CRM вантажів (n8n Cloud, Maersk-трекінг).
- **[[1c-api]]** — доступ до бази 1С Експедитор (чекаємо API від розробника).

## Мапа гілок (git)
- `main` — бот і автоматизація Експедитора (продакшн; сервер тягне саме звідси).
- `claude/financial-reporting-system-i6auod` — фінзвіт (`report/`), НЕ злито. ⚠ Там же непогоджений finance.py — при злитті переносити ТІЛЬКИ `report/`.
- `crm-dispatch` — код диспетчеризації (`dispatch/`), НЕ злито. Продакшн живе в n8n Cloud, не в репо.
- `claude/repository-visibility-08xxop` — реалізація Maersk-деплою (`dispatch/`, `dispatch-planning/`).
- `claude/video-summary-highlights-arz176`, `claude/claude-rate-limits-QPyVJ` — НЕ злиті.
- `claude/llm-wiki-architecture-z2jrgv` — ця вікі.

## Ключові методики (як рахувати)
- [[zamorozheno-v-ugodah]] — заморожений капітал в угодах.
- [[dso-dpo]] — реальні терміни оплати (замість «запланованої дати» 1С).
- [[likvidnist-po-kasah]] — ліквідність по касах (каси НЕ взаємозамінні!).
- [[kasovyy-rozryv]] — прогноз касового розриву на 30 днів.
- [[mapping-1c]] — мапінг колонок експортів Експедитора.

## Сутності
- [[klienty]] — клієнти (дебіторка, DSO, маржа).
- [[postachalnyky]] — постачальники (кредиторка).
- [[kasy]] — 13 кас/рахунків у 3 валютах.

## Відкриті питання (глобально)
- **1С**: чекаємо адресу і доступи до API від розробника Експедитора (див. [[1c-api]]).
- **Фінзвіт**: місячний прогноз надходжень через ETA контейнерів; Drive-вивантаження перевіряти на старті сесії.
- **Диспетчеризація**: інферити «доставлено» лише зі статусу «Завантажений на авто/потяг» НЕ можна — це ставить людина (див. [[dyspetcheryzatsiya]]).
- **Безпека**: (1) перевипустити ключ Maersk API; (2) керівничий дашборд-вебхук n8n без авторизації.

## Постійні правила роботи
Найвищий пріоритет — «Головні правила» у CLAUDE.md (не робити нічого без запиту/підтвердження;
тільки факти; ніяких секретів у репо; пряма команда важливіша за моє рішення).
