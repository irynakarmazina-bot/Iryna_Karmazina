{"id": "registers-list-0731", "cmd": "cd /root/unitex-finrep && python3 - <<'EOF'\nimport sys,re\nsys.path.insert(0,\"engine\")\nfrom odata_client import ODataClient\nc=ODataClient()\nes=list(c.entity_sets())\nprint(\"усього наборів:\",len(es))\nfor pref in (\"AccountingRegister\",\"AccumulationRegister\",\"InformationRegister\",\"ChartOfAccounts\",\"Document_\"):\n    got=[e for e in es if e.startswith(pref)]\n    print(\"\\n== %s (%d) ==\"%(pref,len(got)))\n    print(\", \".join(got[:40]) if got else \"  НЕМАЄ\")\nEOF"}