{"id": "sync-copy-1", "cmd": "set -e; R=/root/Iryna_Karmazina/direct_sync; D=/root/direct-sync; cd /root/Iryna_Karmazina && git pull -q && echo 'репо оновлено'; mkdir -p $D/.backups; TS=$(date +%Y%m%d-%H%M%S); for f in maersk_track_sync.py expeditor_direct_sync.py cosco_track_sync.py; do   [ -f $D/$f ] && cp -p $D/$f $D/.backups/$f.$TS && echo \"бекап $f\"; done; for f in maersk_track_sync.py expeditor_direct_sync.py cosco_track_sync.py runlock.py; do   cp $R/$f $D/$f && echo \"скопійовано $f\"; done; echo '--- звірка ---'; for f in maersk_track_sync.py expeditor_direct_sync.py cosco_track_sync.py runlock.py; do   printf '%-30s ' $f; cmp -s $D/$f $R/$f && echo ЗБІГАЄТЬСЯ || echo РІЗНІ; done; echo -n 'Напрямок у READ_FIELDS на сервері: '; grep -c 'Напрямок' $D/maersk_track_sync.py"}