{"id": "track-dates-log-0801", "cmd": "for i in $(seq 1 11); do pgrep -f maersk_track_sync >/dev/null || break; sleep 10; done; tail -n 6 /root/track_dates.log; echo '--- заповненість дат ---'; python3 -c \"
import json,urllib.request
TOK=open('/root/nocodb-token.txt').read().strip()
def g(p):
    r=urllib.request.Request('http://localhost:8080'+p,headers={'xc-token':TOK}); return json.load(urllib.request.urlopen(r,timeout=120))
rows=[];off=0
while True:
    js=g('/api/v2/tables/m58xsjo6at01ohl/records?limit=200&offset=%d'%off); rows+=js.get('list',[])
    if js.get('pageInfo',{}).get('isLastPage'): break
    off+=200
F=lambda r,k: str(r.get(k) or '').strip()
for f in ['Гейт ін','Гейт аут','ETD (факт)','ETD (план)','Вивантаження в порту (факт)','ETA порт (факт)']:
    print('   %-28s %d з %d'%(f, sum(1 for r in rows if F(r,f)), len(rows)))
\""}
