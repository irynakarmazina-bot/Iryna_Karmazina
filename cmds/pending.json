{"id": "check-236", "cmd": "echo '--- підсумок прогону ---'; tail -6 /tmp/m236.log; echo; echo '--- угода 236 у платформі ЗАРАЗ ---'; python3 -c \"import json,urllib.request;t=open('/root/nocodb-token.txt').read().strip();r=urllib.request.Request('http://localhost:8080/api/v2/tables/m58xsjo6at01ohl/records?limit=1000',headers={'xc-token':t});d=[x for x in json.load(urllib.request.urlopen(r))['list'] if str(x.get('Угода')).strip()=='236'];print(json.dumps({k:d[0].get(k) for k in ['Угода','Статус','ETA','ETA порт (план)','ETA порт (факт)','Судно','Зміни ETA (історія)']},ensure_ascii=False,indent=1) if d else 'не знайдено')\""}