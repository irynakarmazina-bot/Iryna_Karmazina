{"id": "done-field-1", "cmd": "python3 - <<'PYEOF'\n# -*- coding: utf-8 -*-\nimport json, urllib.request\nTOK = open('/root/nocodb-token.txt').read().strip()\nr = urllib.request.Request('http://localhost:8080/api/v2/tables/m58xsjo6at01ohl/records?limit=500', headers={'xc-token': TOK})\nrows = json.load(urllib.request.urlopen(r))['list']\nf1, f2, f3 = 'Вивантаження у отримувача (факт)', 'Планова до клієнта (факт)', 'Планова до клієнта (план)'\nc1 = sum(1 for x in rows if str(x.get(f1) or '').strip())\nc2 = sum(1 for x in rows if str(x.get(f2) or '').strip())\nc3 = sum(1 for x in rows if str(x.get(f3) or '').strip())\ndone = [x for x in rows if str(x.get('Статус')) == 'Вантаж доставлено']\nd1 = sum(1 for x in done if str(x.get(f1) or '').strip())\nd2 = sum(1 for x in done if str(x.get(f2) or '').strip())\nprint('усього рядків: %d, доставлених: %d' % (len(rows), len(done)))\nprint('«%s»: заповнено %d (у доставлених %d)' % (f1, c1, d1))\nprint('«%s»: заповнено %d (у доставлених %d)' % (f2, c2, d2))\nprint('«%s»: заповнено %d' % (f3, c3))\nPYEOF"}