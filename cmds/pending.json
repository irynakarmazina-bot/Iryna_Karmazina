{"id": "sync-verify-1", "cmd": "cd /root/direct-sync && PY=/root/unitex-finrep/.venv/bin/python; echo '=== 1. пробний прогін Maersk (без запису) ==='; $PY maersk_track_sync.py --dry-run --limit 3 2>&1 | tail -12; echo '=== 2. чи бачить скрипт Напрямок ==='; $PY -c \"import sys; sys.argv=['x']; exec(open('/root/direct-sync/maersk_track_sync.py').read().split('def main')[0]); rows=nc_records(); n=sum(1 for r in rows if 'Напрямок' in r); print('угод усього:', len(rows), '| з полем Напрямок:', n); print('експортних:', sum(1 for r in rows if str(r.get('Напрямок') or '').strip()=='Експорт'))\" 2>&1 | tail -5; echo '=== 3. замок працює? два запуски одночасно ==='; ($PY maersk_track_sync.py --dry-run --limit 1 >/tmp/l1.txt 2>&1 &) ; sleep 1; $PY maersk_track_sync.py --dry-run --limit 1 2>&1 | grep -i 'ВЖЕ ВИКОНУЄТЬСЯ' || echo '(другий запуск не був заблокований — перший міг уже завершитись)'; sleep 4; echo '--- перший завершився: ---'; tail -3 /tmp/l1.txt"}