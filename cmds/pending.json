{"id": "check-236b", "cmd": "for i in $(seq 1 10); do grep -q 'MAERSK_OK\\|MAERSK_DRY' /tmp/m236.log && break; sleep 10; done; echo '--- підсумок ---'; tail -4 /tmp/m236.log; echo; echo '--- угода 236 ---'; python3 -c \"import json,urllib.request;t=open('/root/nocodb-token.txt').read().strip();r=urllib.request.Request('http://localhost:8080/api/v2/tables/m58xsjo6at01ohl/records?limit=1000',headers={'xc-token':t});d=[x for x in json.load(urllib.request.urlopen(r))['list'] if str(x.get('Угода')).strip()=='236'];print(json.dumps({k:d[0].get(k) for k in ['Угода','Статус','ETA','ETA порт (план)','ETA порт (факт)']},ensure_ascii=False)) if d else print('нема')\""}