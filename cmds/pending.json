{"id": "d52a6373944b", "cmd": "python3 - <<'PY'\nimport csv, collections\nf='/root/unitex-finrep/normalized/supplier_invoices.csv'\nrows=[r for r in csv.DictReader(open(f,encoding='utf-8'))]\nm=[r for r in rows if 'maersk' in (r['supplier'] or '').lower() or 'мерск' in (r['supplier'] or '').lower()]\nprint('статуси:', collections.Counter(r['invoice_status'] for r in m))\nprint('порожня стаття (service):', sum(1 for r in m if not (r['service'] or '').strip()))\nprint('порожній вид оплати:', sum(1 for r in m if not (r['payment_method'] or '').strip()))\nprint('унікальних угод:', len(set(r['deal'] for r in m)))\nprint('діапазон дат:', min(r['date'] for r in m), '→', max(r['date'] for r in m))\nprint('останні 8 рахунків Maersk за датою:')\nfor r in sorted(m, key=lambda r: r['date'])[-8:]:\n    print('  ', r['date'], r['number'], r['amount'], r['currency'], '|', r['deal'][:34], '| стаття:', repr(r['service']), '|', r['invoice_status'])\nPY\n"}