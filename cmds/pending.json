{"id": "check-38", "cmd": "python3 -c \"\nimport json,urllib.request\ntok=open('/root/nocodb-token.txt').read().strip()\nrows=[];off=0\nwhile True:\n    r=urllib.request.Request('http://localhost:8080/api/v2/tables/m58xsjo6at01ohl/records?limit=200&offset=%d'%off,headers={'xc-token':tok})\n    js=json.loads(urllib.request.urlopen(r,timeout=60).read().decode()); rows+=js['list']\n    if len(js['list'])<200: break\n    off+=200\nm=[r for r in rows if r.get('Переказ за кордон')]\nprint('позначено угод:',len(m),'· сума:',round(sum(float(r.get('Сума переказу') or 0) for r in m),2))\nfor r in sorted(m,key=lambda x:int(x['Угода']))[:6]:\n    print('   угода %-4s сума %8.2f дата %s' % (r['Угода'], float(r.get('Сума переказу') or 0), str(r.get('Дата переказу'))[:10]))\n\""}