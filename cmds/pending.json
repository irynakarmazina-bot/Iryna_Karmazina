{"id": "maersk-data-probe-0802", "cmd": "cd /root/unitex-finrep && python3 -c \"
import csv,os,collections
d='normalized'
for f in sorted(os.listdir(d)):
    if f.endswith('.csv'):
        with open(os.path.join(d,f),encoding='utf-8') as fh:
            h=fh.readline().strip()
        print(f,'|',h[:220])
print()
p=os.path.join(d,'cash_moves.csv')
rows=list(csv.DictReader(open(p,encoding='utf-8')))
print('cash_moves рядків:',len(rows))
hits=[r for r in rows if any('аерск' in str(v) or 'aersk' in str(v) for v in r.values())]
print('рядків зі згадкою Маерск:',len(hits))
if hits:
    print('приклад:',{k:v for k,v in list(hits[0].items())[:14]})
    cols=collections.Counter()
    for r in hits:
        for k,v in r.items():
            if 'аерск' in str(v) or 'aersk' in str(v): cols[k]+=1
    print('де саме згадка:',dict(cols))
\" 2>&1 | base64 -w0"}
