{"id": "smtp-check-1", "cmd": "echo '=== SMTP у NocoDB (для «Забули пароль») ==='; systemctl show nocodb -p Environment 2>/dev/null | tr ' ' '\\n' | grep -i 'SMTP\\|MAIL' | sed 's/=.*/=<приховано>/' | head; grep -ril 'NC_SMTP' /root --include='*.env' --include='docker-compose*' 2>/dev/null | head -3; echo '(порожньо = пошта не налаштована)'"}