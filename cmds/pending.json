{"id": "why274e-0805", "cmd": "python3 -c \"import json,urllib.request,urllib.parse;tok=open('/root/nocodb-token.txt').read().strip();u='http://localhost:8080/api/v2/tables/m58xsjo6at01ohl/records?limit=1000&fields='+urllib.parse.quote('Угода,Статус,Етап (Експедитор),UpdatedAt,CreatedAt');r=urllib.request.Request(u,headers={'xc-token':tok});rows=json.load(urllib.request.urlopen(r,timeout=90)).get('list',[]);print('усього рядків:',len(rows));print('з них UpdatedAt заповнено:',sum(1 for x in rows if x.get('UpdatedAt')));bad=[x for x in rows if str(x.get('Статус'))=='Вантаж доставлено' and str(x.get('Етап (Експедитор)')) in ('Букинг','Выполняется','ВыставленСчет')];print('ДОСТАВЛЕНО, але етап в Експедиторі ще робочий:',len(bad));[print('  угода',x.get('Угода'),'| етап',x.get('Етап (Експедитор)'),'| змінено',x.get('UpdatedAt')) for x in sorted(bad,key=lambda y:-int(str(y.get('Угода') or 0) if str(y.get('Угода') or '0').isdigit() else 0))[:15]]\" 2>&1 | tail -25"}