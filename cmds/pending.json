{"id": "why256-0805", "cmd": "python3 -c \"import sys,json,datetime;sys.path.insert(0,'/root/direct-sync');import maersk_track_sync as M;rows=[r for r in M.nc_records() if str(r.get('Угода'))=='256'];print('ЗАПИС У ПЛАТФОРМІ:');print(json.dumps({k:rows[0].get(k) for k in ('Угода','Статус','Етап (Експедитор)','Судно','ETA','ETA порт (план)','ETA порт (факт)','Вивантаження в порту (факт)','Гейт аут','BL','Контейнер','Останнє оновлення','Остання зміна')},ensure_ascii=False,indent=1));print('ВАРІАНТИ КОЛОНКИ СТАТУС:',sorted(M.nc_status_options()));env=M.load_env();tok=M.maersk_token(env);evs=M.collect_events(env,tok,rows[0].get('BL'),(rows[0].get('Контейнер') or '').split(',')[0].strip());today=datetime.date.today().isoformat();act=[e for e in evs if e.get('eventClassifierCode')=='ACT' and M._dt(e)[:10]<=today];print('подій усього:',len(evs),'| фактичних і вже минулих:',len(act));print('ОСТАННІ 6 ФАКТИЧНИХ ПОДІЙ (дата, код, транспорт):');[print('  ',M._dt(e)[:16],(e.get('equipmentEventTypeCode') or e.get('transportEventTypeCode') or e.get('shipmentEventTypeCode')),((e.get('transportCall') or {}).get('modeOfTransport') or '')) for e in sorted(act,key=M._dt)[-6:]]\" 2>&1 | tail -30"}