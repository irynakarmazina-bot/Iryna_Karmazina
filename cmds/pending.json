{"id": "why256b-0805", "cmd": "python3 -c \"import sys,json,datetime;sys.path.insert(0,'/root/direct-sync');import maersk_track_sync as M;r=[x for x in M.nc_records() if str(x.get('Угода'))=='256'][0];env=M.load_env();tok=M.maersk_token(env);evs,how,note=M.collect_events(env,tok,r.get('BL'),(r.get('Контейнер') or '').split(',')[0].strip());today=datetime.date.today().isoformat();print('як знайшли:',how,'| нота:',note,'| подій усього:',len(evs));act=[e for e in evs if e.get('eventClassifierCode')=='ACT' and M._dt(e)[:10]<=today];print('фактичних і вже минулих:',len(act));print('ОСТАННІ ФАКТИЧНІ ПОДІЇ (дата | код | транспорт):');[print('  ',M._dt(e)[:16],'|',(e.get('equipmentEventTypeCode') or e.get('transportEventTypeCode') or e.get('shipmentEventTypeCode')),'|',((e.get('transportCall') or {}).get('modeOfTransport') or '')) for e in sorted(act,key=M._dt)[-8:]];print('ЩО ПОРАХУВАВ БИ ТРЕКІНГ:');w=M.parse_events(evs,r,today,M.nc_status_options());print(json.dumps({k:w.get(k) for k in ('Статус','Судно','ETA порт (план)','ETA порт (факт)','Вивантаження в порту (факт)','Гейт аут')},ensure_ascii=False,indent=1))\" 2>&1 | tail -30"}