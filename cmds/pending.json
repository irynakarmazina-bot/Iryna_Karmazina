{"id": "sheet-why", "cmd": "echo '=== коли востаннє викликали /cash-sheet ==='; grep -c 'cash-sheet' /var/log/caddy/*.log 2>/dev/null | head -3 || echo '(логів Caddy немає або порожні)'; echo; echo '=== чи є в run.sh або cron ==='; grep -rn 'sheet' /root/unitex-finrep/run.sh 2>/dev/null | head -5 || echo '(у run.sh немає)'; crontab -l 2>/dev/null | grep -i 'sheet\\|cash' || echo '(у cron немає)'; echo; echo '=== що за таблиця (без ключів) ==='; python3 -c \"import json;c=json.load(open('/root/unitex-finrep/secure/config.json'));print('spreadsheet_id є:', bool(c.get('spreadsheet_id')));print('n8n webhook налаштовано:', bool((c.get('n8n') or {}).get('webhook_url')))\" 2>&1 | head -4; echo; echo '=== чи є ще щось, що пише в Google ==='; ls -1 /root/unitex-finrep/engine/ | head -20"}