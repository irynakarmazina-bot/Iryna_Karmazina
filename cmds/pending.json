{"id": "recon-catalogs-all-0730", "cmd": "cd /root/unitex-finrep && python3 -c \"\nimport sys;sys.path.insert(0,'engine')\nfrom odata_client import ODataClient\nprint(' '.join(e for e in ODataClient().entity_sets() if e.startswith('Catalog_')))\" | fold -w 2900 | head -1"}