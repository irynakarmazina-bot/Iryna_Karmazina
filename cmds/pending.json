{"id": "kasa-audit-3-0802", "cmd": "python3 -c \"import csv,re,collections;P=re.compile('аерск|aersk',re.I);rows=list(csv.DictReader(open('/root/unitex-finrep/normalized/cash_moves.csv',encoding='utf-8')));kasa=[r for r in rows if P.search(str(r.get('payment_method') or ''))];pays=[r for r in kasa if (r.get('expense_uo') or '').strip() not in ('','0')];agg=collections.defaultdict(lambda:[0.0,0]);print('рухів через касу:',len(kasa),'| виплат:',len(pays));print('назви каси:',dict(collections.Counter((r.get('payment_method') or '').strip() for r in kasa)));[agg[(r.get('counterparty') or '(порожньо)').strip()].__setitem__(0, agg[(r.get('counterparty') or '(порожньо)').strip()][0]+float(str(r.get('expense_uo') or 0).replace(',','.'))) or agg[(r.get('counterparty') or '(порожньо)').strip()].__setitem__(1, agg[(r.get('counterparty') or '(порожньо)').strip()][1]+1) for r in pays];print('ОТРИМУВАЧІ виплат із каси:');[print('  %12.2f %4d  %s'%(v[0],v[1],k[:55])) for k,v in sorted(agg.items(),key=lambda x:-x[1][0])];print('РАЗОМ: %.2f'%sum(v[0] for v in agg.values()))\" 2>&1 | tail -25"}