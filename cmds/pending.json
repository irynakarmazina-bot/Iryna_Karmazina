{"id": "check2-0805", "cmd": "for i in $(seq 1 20); do pgrep -f maersk_track_sync >/dev/null 2>&1 || break; sleep 5; done; echo '--- ПІДСУМОК ПРОГОНУ ---'; tail -4 /root/direct-sync/run-0805.txt; echo '--- СТАН ДАНИХ ---'; python3 -c \"import json,urllib.request,urllib.parse as P;tok=open('/root/nocodb-token.txt').read().strip();B='http://localhost:8080/api/v2/tables/m58xsjo6at01ohl/records';h={'xc-token':tok};rows=json.load(urllib.request.urlopen(urllib.request.Request(B+'?limit=1000&fields='+P.quote('Угода,Статус,Статус (джерело),Статус (оновлено),Трекінг (стан),ETA'),headers=h),timeout=90)).get('list',[]);sil=[x for x in rows if 'немає даних' in str(x.get('Трекінг (стан)') or '')];oth=[x for x in rows if 'інша лінія' in str(x.get('Трекінг (стан)') or '')];arr=[x for x in rows if x.get('Статус')=='Прибув у порт'];srcd=[x for x in rows if x.get('Статус (джерело)')];print('трекінг мовчить:',len(sil),[x.get('Угода') for x in sil]);print('інша лінія:',len(oth),[x.get('Угода') for x in oth][:12]);print('«Прибув у порт»:',len(arr),[x.get('Угода') for x in arr]);print('із проставленим джерелом:',len(srcd));print('УГОДА 256:',json.dumps([x for x in rows if str(x.get('Угода'))=='256'][0],ensure_ascii=False))\" 2>&1 | tail -8"}