{"id":"filelink-fix-1","cmd":"docker inspect caddy --format '{{.HostConfig.NetworkMode}}' ; docker inspect caddy --format '{{range .Mounts}}{{.Source}} -> {{.Destination}}{{println}}{{end}}' | head -6; echo '--- чи збігається конфіг у контейнері з /root/Caddyfile ---'; docker exec caddy sh -c 'cat /etc/caddy/Caddyfile' > /tmp/caddy_in_container.txt 2>&1 && (diff -q /tmp/caddy_in_container.txt /root/Caddyfile && echo ОДНАКОВІ || echo РІЗНІ) ; grep -n 'dltemp' /tmp/caddy_in_container.txt || echo 'dltemp у конфігу немає'"}
