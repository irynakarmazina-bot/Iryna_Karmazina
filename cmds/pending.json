{"id": "check-0805", "cmd": "echo '--- ЧИ ЗАВЕРШИВСЯ ТРЕКІНГ ---'; tail -3 /root/direct-sync/run-0805.txt; echo '--- СТАН ДАНИХ ---'; python3 -c \"import json,urllib.request,urllib.parse as P;tok=open('/root/nocodb-token.txt').read().strip();B='http://localhost:8080/api/v2/tables/m58xsjo6at01ohl/records';h={'xc-token':tok};g=lambda q: json.load(urllib.request.urlopen(urllib.request.Request(B+'?limit=1000&'+q,headers=h),timeout=90)).get('list',[]);rows=g('fields='+P.quote('Угода,Статус,Статус (джерело),Статус (оновлено),Трекінг (стан),ETA'));print('усього угод:',len(rows));src={};[src.update({str(x.get('Статус (джерело)') or 'не проставлено'):src.get(str(x.get('Статус (джерело)') or 'не проставлено'),0)+1}) for x in rows];print('джерело статусу:',src);sil=[x for x in rows if 'немає даних' in str(x.get('Трекінг (стан)') or '')];oth=[x for x in rows if 'інша лінія' in str(x.get('Трекінг (стан)') or '')];print('трекінг мовчить (немає даних):',len(sil),[x.get('Угода') for x in sil]);print('контейнер іншої лінії:',len(oth));arr=[x for x in rows if x.get('Статус')=='Прибув у порт'];print('зі статусом «Прибув у порт»:',len(arr),[x.get('Угода') for x in arr]);r256=[x for x in rows if str(x.get('Угода'))=='256'][0];print('УГОДА 256:',json.dumps(r256,ensure_ascii=False))\" 2>&1 | tail -12"}