{"id": "why274b-0805", "cmd": "python3 -c \"import sys,os,json;os.chdir('/root/unitex-finrep');sys.path.insert(0,'/root/unitex-finrep/engine');from odata_client import ODataClient;c=ODataClient();hit=[d for d in c.list('Document_Сделка') if str(d.get('Number') or '').strip().lstrip('0')=='274'];print('ЗНАЙДЕНО В ЕКСПЕДИТОРІ:',len(hit));[print(json.dumps({k:d.get(k) for k in ('Number','Date','Posted','Статус','ДатаВыполненияРабот','ЕТА','ЕТА_ПОД','ЕТА_ПОЛ','ДатаЗавершения')},ensure_ascii=False,indent=1)) for d in hit]\" 2>&1 | tail -20"}