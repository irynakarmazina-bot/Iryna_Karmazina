{
  "stdout": "=== update_all_api.py: skip_parse / кроки ===\n9:    крок parse НЕ виконується (щоб не затерти API-дані);\n10:  • каси (cash_moves/cash_balances) і накладні витрати лишаються файловими\n28:STEPS = [\n29:    (\"Інгест з API (рахунки/витрати/угоди)\", \"odata_ingest.py\"),\n39:def run(label, script, args=None):\n42:    r = subprocess.run(cmd, capture_output=True, text=True, env=os.environ)\n51:def set_data_date_today():\n52:    \"\"\"Дата даних = сьогодні (API живий). Оновлюємо reports/parse-report.json.\"\"\"\n53:    p = os.path.join(ROOT, \"reports\", \"parse-report.json\")\n61:def main():\n63:    # 0) розбір файлів — потрібен ЛИШЕ заради кас (cash_moves/cash_balances) з\n66:    #    --skip-parse: каси задані вручну (напр. зі скріна) — розбір пропускаємо.\n67:    if \"--skip-parse\" not in sys.argv:\n68:        run(\"Розбір файлів (тільки заради кас)\", \"parse.py\")\n70:        print(\"→ Розбір пропущено (--skip-parse): каси взято як є з normalized/cash_balances.csv\")\n72:    run(STEPS[0][0], STEPS[0][1], args=[os.path.join(ROOT, \"normalized\")])\n75:    for label, script in STEPS[1:]:\n76:        run(label, script)\n\n=== odata_ingest.py: де пише cash_moves ===\n8:Рухи грошей (cash_moves) — ПОКИ НЕ ТУТ: чекаємо, поки розробник підкаже касу в\n27:def _d10(x):\n32:def _dt_str(iso):\n41:def _month_end(iso):\n49:def _num(x):\n56:def _debt(row):\n62:def _name_map(c, entity):\n74:def build_caches(c):\n95:def deal_string(d):\n101:def ingest_income(c, L, out):\n138:def ingest_supplier(c, L, out):\n175:def ingest_deals(c, L, out):\n206:def _write(out, name, rows):\n217:def main(out_dir):\n224:    print(\"Готово (без cash_moves — чекає на касу).\")\n\n",
  "stderr": "",
  "returncode": 0,
  "id": "um-q12",
  "ts": "2026-08-11T10:01:15Z"
}