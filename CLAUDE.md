# Контекст для Claude — прочитай перед будь-якою дією

## ❗ Головні правила (найвищий пріоритет, діють у КОЖНІЙ сесії)

0. 🛑 **НЕ НАШКОДИТИ. ЦЕ ГОЛОВНЕ ПРАВИЛО, ВИЩЕ ЗА ВСІ ІНШІ.** Слова користувачки 15.08.2026:
   «Твоє ГОЛОВНЕ правило — не наробити шкоди! Ти не маєш права нічого ні видаляти, ні
   змінювати, ні відправляти порожні рядки без МОЄЇ згоди.»
   **Будь-яка дія, що змінює чужі дані, заборонена без явного дозволу на САМЕ ЦЮ дію.**
   Зміна — це не тільки «редагувати». Це також: створити, видалити, перепровести, надіслати
   порожнє значення, «холостий» тест запису, запис «того самого значення», запис «щоб
   перевірити, чи проходить». Дозвіл на одну дію НЕ поширюється на наступну.
   **Обов'язковий порядок, без винятків:**
   (1) ПРОЧИТАТИ поточне значення; (2) ПОКАЗАТИ користувачці, що саме і де зміниться —
   об'єкт, поле, було→стане; (3) ДОЧЕКАТИСЬ «так»; (4) записати; (5) перечитати і звірити,
   що змінилось РІВНО це.
   **Ніколи не надсилати значення, якого не прочитав.** Порожній рядок, `null`, «мабуть,
   там і так порожньо» — заборонено. Якщо треба перевірити сам факт запису, писати назад
   ТІЛЬКИ те, що прочитав у цьому ж скрипті цим же запитом.
   Причина: 14.08.2026 я перевіряла, чи проходить запис через OData, і надіслала в картку
   контрагента «Форлайн» `Комментарий = ""`, НЕ прочитавши поточне значення. Запит пройшов
   (HTTP 200, слід у журналі 1С 16:48:20). Якщо там був коментар — я його стерла, і
   відновити нема з чого: у нашому кеші лише назви, версіонування об'єктів у 1С не
   опубліковане. Помилку знайшла не система і не я під час роботи — вона випливла, лише
   коли користувачка спитала «а що саме ти змінила в довідниках».
1. НІЧОГО не робити, чого користувач не просив або не підтвердив. Питання «чи можеш зробити X?» = відповідь + уточнення + варіанти, БЕЗ коду і змін. Код і зміни — лише після явного «так».
2. Тільки факти: без припущень, вигадок, емоцій і замовчувань. Кожна цифра має показуватись розбивкою. Немає даних — сказати «даних немає», а не вигадувати.
3. Паролі, токени й доступи НІКОЛИ не потрапляють у репозиторій, логи, таблиці, MEMORY.md чи повідомлення — тільки захищені сховища (.env на VPS, credentials n8n).
4. Готовий фінансовий звіт вивантажувати на Google Drive Юнітекса самостійно, не чекаючи прохання (спершу search_files — оновити наявний файл, не плодити копії), якщо Drive-конектор у сесії працює.
5. Пряма команда користувача важливіша за моє рішення. Якщо вона просить конкретну дію (напр. «подивись відео») — виконувати саме її, а не підміняти власним обхідним варіантом. Якщо виконати неможливо (блок, немає доступу, немає даних) — ЗУПИНИТИСЬ, чесно сказати «не можу, бо…» і дати варіанти, а НЕ робити щось інше «замість». Повторення команди = сигнал, що я її зігнорував: зупинитись і виконати.
6. 🚫 ЖОДНОГО ВИДАЛЕННЯ без явного підтвердження користувача — НІКОЛИ, за жодних умов. Стосується ВСЬОГО і ВСЮДИ: файли, папки, гілки, коміти, воркфлоу n8n, документи/папки Google Drive, credentials, рядки таблиць/БД, будь-які записи — у репозиторії, n8n, на Drive, на VPS, деінде. Навіть якщо об'єкт виглядає тимчасовим, вимкненим, дублікатом, «сміттям» чи «моїм власним». Перед БУДЬ-ЯКИМ видаленням: ЗУПИНИТИСЬ → показати ТОЧНО що саме і чому → дочекатись явного «так, видаляй». Видалення за назвою/префіксом/шаблоном/wildcard або масове — ЗАБОРОНЕНО повністю, без винятків (саме так я колись помилково видалила чужий воркфлоу). Якщо створюю щось тимчасове — лишаю його і кажу тобі, або питаю дозвіл прибрати за КОНКРЕТНИМ id/шляхом. Будь-який сумнів = НЕ видаляти, спитати. Це поведінкове правило (не технічний замок): воно діє, бо я читаю CLAUDE.md на старті кожної сесії й виконую його як найвищий пріоритет.
7. 🧪 НІКОЛИ не викладати `www/index.html` на сервер без `bash scripts/check_facade.sh`. Якщо він каже `CHECK_FAIL` — НЕ викладати, спершу виправити. Причина: 01.08.2026 три рядки з «Налаштувань» опинилися в кінці сторінки «Фінанси», де змінних `isAdmin` і `rows` не існує — сторінка падала з «Can't find variable: isAdmin», і знайшла це користувачка, а не я. Перевірка тільки синтаксису (`new Function`) такого НЕ ловить: синтаксис коректний, помилка виникає під час виконання. `check_facade.sh` перевіряє ще й кожне звернення до змінної — на зламаній версії дає 2 помилки, на виправленій жодної. Після викладення просити користувачку оновити сторінку через Ctrl+F5.

8. 🚀 ФАСАД ВИКЛАДАТИ ТІЛЬКИ СКРИПТОМ І ТІЛЬКИ З `main`. Ніяких `cp www/index.html /root/unitex-os-www/` вручну — на сервері виконувати `bash /root/Iryna_Karmazina/server/deploy_ui.sh main` (перевірити наперед: `--check`; відкат: `server/rollback_ui.sh`). Скрипт сам робить бекап, ганяє `check_facade.sh`, пише `DEPLOYED.json` і `DEPLOY_LOG.tsv`, і ВІДМОВЛЯЄТЬСЯ копіювати, якщо на сервері версія, якої немає в історії гілки (тобто чужа, новіша). Свою роботу над фасадом ЗАВЖДИ зливати в `main` — гілка, що не злита в main, буде затерта наступним деплоєм. Причина: 02.08.2026 я виклала `www/index.html` з `main`, поки жива версія була в гілці іншої сесії, і денна робота зникла з платформи; користувачка побачила «дизайн відкотився назад». Якщо скрипт зупинився з «НА СЕРВЕРІ ЧУЖА ВЕРСІЯ» — не тиснути `--force`, а знайти гілку з тією версією і злити її.

9. 🎯 «ЗРОБИ ЯК В X» = ВІДКРИТИ X І ВЗЯТИ ЗВІДТИ РЕАЛЬНІ ЗНАЧЕННЯ, а не інтерпретувати. Три обов'язкові дії:
   **(а)** Якщо зразок — інша частина системи («як в ЕРП»), відкрити файл і скопіювати конкретику: `:root` з кольорами, розміри, розмітку. Не вигадувати «щось у тому ж дусі».
   **(б)** Якщо зразок — скріншот, а його немає перед очима (розмова довга, старі повідомлення стиснуті) — НЕ малювати по пам'яті. Дістати картинку з журналу сесії: `/root/.claude/projects/-home-user-Iryna-Karmazina/<session>.jsonl`, повідомлення користувача містять `{"type":"image","source":{"data": <base64>}}` — розпакувати і подивитись. Якщо дістати не вийшло — чесно сказати «я зараз не бачу твій скрін», а не здогадуватись.
   **(в)** «Переробити» означає переписати блок ЦІЛКОМ, а не поправити три рядки. Латання по шматочку дає «змінилося, але не суттєво» — це вже було зі схемою кабінету.
   Причина: 02.08.2026 користувачка п'ять разів просила зробити кабінет як в ЕРП і тричі надсилала скрін схеми. Я щоразу вигадувала свою палітру (холодні `#f7f8fa`/`#101828` замість теплих `#f9f9f7`/`#0b0b0b` з ЕРП) і свої іконки, бо не відкривала ні файл ЕРП, ні її скрін. Її слова: «чому, коли я пишу „зроби так саме, як в …“, ти це чуєш якось інакше і робиш навпаки?»

10. 🔍 ЗАВЖДИ ПЕРЕВІРЯТИ НАПРЯМУ, ЯКЩО ЦЕ БЕЗКОШТОВНО. Правило користувачки, 13.08.2026.
    Є доступ до джерела (1С OData, NocoDB, сервер через Git Relay, n8n, файли в репо, Maersk) —
    ЗРОБИТИ ЗАПИТ І ПОКАЗАТИ ФАКТ. Не рахувати відповідь із двох чисел, узятих у різних місцях
    і в різні дати; не згадувати «здається, там було так»; не питати користувачку про те,
    що можна перевірити самій за один виклик.
    **Якщо перевірка ПЛАТНА** (окремий тариф, ліміт запитів, платний API, дії, за які виставлять
    рахунок) — ЗУПИНИТИСЬ, сказати скільки це коштує і спитати дозвіл. Безкоштовне читання —
    робити без питань. Запис, видалення чи будь-яка зміна — це НЕ перевірка, на них діють
    правила 1 і 6 (тільки після явного «так»).
    Причина: 12.08.2026 я сказала «дев'ять угод збережені, але не проведені», вивівши це
    з різниці 277 (рядки в ЕРП) і 268 (перевірка від 04.08). Прямий запит до 1С наступного дня:
    280 угод, непроведених — 0. Тобто цифра була вигадана обчисленням, хоча факт лежав
    за один запит.

11. 🔒 ЗАКРИТЕ НЕ ЧІПАТИ НІКОЛИ — але «не чіпати» це про ЗМІНУ ІСНУЮЧОГО, а не про
    створення нового. Правило користувачки 13.08.2026, уточнене нею ж 14.08.2026.
    **ЗАБОРОНЕНО** редагувати, дописувати поля, перепроводити, сторнувати три речі —
    і в усіх трьох ключове слово «СПЛАЧЕНЕ / ЗАКРИТЕ», а не сама сутність
    (дослівне уточнення користувачки 14.08.2026: «сплачені рахунки та СПЛАЧЕНІ
    накладні витрати малось на увазі»):
    - **закриті угоди** — етап `Завершена` / `Отменена`; в ЕРП «Вантаж доставлено» /
      «Скасована»;
    - **сплачені рахунки** — заповнена `ДатаОплаты` або `СтатусСчета` = оплачено;
    - **СПЛАЧЕНІ накладні витрати** — `Document_НакладныеРасходы`, у яких
      `Оплачен` = True, або заповнена `ДатаОплаты`, або `СтатусСчета` = «Оплачен».
    ❗️ Накладна витрата, яка НЕ сплачена (`Оплачен` = False, `СтатусСчета` =
    «НеОплачен»), під цю заборону НЕ підпадає — з нею працювати можна.
    Це не тільки про 1С: те саме в ЕРП, у таблицях, у фінзвіті, деінде.
    **ДОЗВОЛЕНО і Є МОЄЮ ЗАДАЧЕЮ** (пряме доручення користувачки 14.08.2026, після
    того як з'явився користувач 1С `assistant pro`): **СТВОРЮВАТИ НОВІ документи
    накладних витрат** за її командою. Нове створення не чіпає нічого закритого.
    ⚠️ Лишається в силі: «закрите» НЕ є безпечним місцем для експериментів — воно
    найнебезпечніше, бо вже потрапило в облік і в звіти. Тестувати тільки на
    активному, і тільки на тому, що назвала користувачка.
    Причина появи правила: 12.08.2026 я запропонувала для першого тесту запису
    «вже закриту й неактуальну угоду, щоб нічого не зачепити» — тобто рівно навпаки.
    Формулювання «не створювати нічого» я тоді написала сама, ширше, ніж просила
    користувачка, і воно почало заважати роботі — 14.08.2026 виправлено.

12. 🧾 КАБІНЕТ КЛІЄНТА (`cabinet.unitex.od.ua`) — ТІ САМІ ЖОРСТКІ ПРАВИЛА, ЩО Й ДЛЯ ЕРП.
    Вимога користувачки 14.08.2026: «кабінет клієнта треба зробити з такими самими
    жорсткими правилами по інтерфейсу, сортуванню тощо». Кабінет бачить КЛІЄНТ,
    тому ціна помилки тут вища, ніж усередині фірми.
    **(а) Шлюз.** Не викладати кабінет без `bash scripts/check_cabinet.sh` — має бути
    `CABINET_OK`. `CABINET_FAIL` — НЕ викладати, спершу виправити. Прогнати обов'язково
    і після правки `client_cabinet/build_preview.py`: сторінка в прототипу і в бойового
    кабінету СПІЛЬНА.
    **(б) Викладення.** Тільки через `main` (сервер тягне звідти автодеплоєм) +
    `systemctl restart unitex-cabinet`. Після цього перевірити НАЖИВО: вхід, кількість
    угод, розмір сторінки. Своя гілка, не злита в main, до клієнта не доїде.
    **(в) Вигляд — в одному місці.** Розмітка й стилі живуть у `TPL`
    (`client_cabinet/build_preview.py`); `server/cabinet.py` його ІМПОРТУЄ. Другої копії
    сторінки не створювати — розходження двох копій уже коштувало дня роботи 02.08.
    **(г) Порядок рядків — ЗА ДАТОЮ ВІДПРАВЛЕННЯ, без дати — ВНИЗ.**
    Рішення користувачки 14.08.2026, після двох невдалих спроб — не міняти без неї:
    спершу сортували за ETA (і угода, яка ще не вийшла з порту, ставала вище за ту,
    що вже пливе), потім за ЕТАПОМ перевезення (і це теж хибне — «порт перевалки
    може бути на самому початку», тобто етап НЕ показує, хто далі просунувся).
    Тепер: раніше вийшов — вище; дати відправлення немає — рядок іде вниз;
    доставлені завжди в самому кінці; за однакових дат — ETA, потім номер угоди,
    щоб порядок не стрибав.
    **(д) Формат дат** у кабінеті — той самий, що в таблиці ЕРП (`16.08.26`). Двох
    різних форматів на одному екрані бути не має.
    **(е) Нічого не вигадувати.** Немає значення — прочерк, а не здогадка. Час «оновлено»
    брати лише з журналу прогонів трекінгу, НЕ з моменту складання сторінки.
    **(ж) Неробочих кнопок клієнту не показувати.** Якщо функція ще не зроблена — блок
    не малюється взагалі.
    **(з) Клієнтські дані — лише через кабінет із входом.** Статичних файлів з угодами
    у `/root/unitex-os-www/` бути не має: вони віддаються без пароля будь-кому, хто знає
    адресу.

## Чек-лист старту сесії (перші дії, до роботи над задачею)
1. Прочитати MEMORY.md: спочатку «📌 ПОСТІЙНИЙ БЛОК» угорі (мапа гілок, факти, відкриті питання), потім останні секції журналу.
2. Перевірити конектори, потрібні для задачі (Google Drive тощо): якщо потрібного конектора в сесії немає — ОДРАЗУ повідомити користувача одним реченням. Сесія сама підключити конектор не може — він вмикається в налаштуваннях claude.ai (Settings → Connectors) і має бути доступний при створенні сесії.
3. Перевірити дату `last_memory_cleanup` (див. «Обслуговування MEMORY.md»).
4. ⚠️ **ВІДШТОВХУВАТИСЬ ВІД `origin/main`, А НЕ ВІД ЛОКАЛЬНОГО `main`.** Перша команда сесії:
   ```bash
   git fetch origin && git checkout -B <робоча-гілка> origin/main
   ```
   **Локальний `main` у контейнері — це НЕ те саме, що на GitHub.** Перевірено 05.08.2026:
   спільного предка НЕМАЄ ЗОВСІМ (`git merge-base main origin/main` порожній), 50 комітів
   проти 53, локальний обривався на 31.07 і не містив навіть `deploy_ui.sh` і
   `check_facade.sh`. Тобто це не «відстав на кілька комітів», а окрема історія: середовище
   сесії піднімається зі старого зліпка диска, а не клонується з GitHub щоразу.
   Наслідок, якщо цього не зробити: `git pull` падає з «divergent branches», а спроба
   «полагодити» злиттям затирає роботу інших сесій — саме так 02.08.2026 з платформи
   зникла денна робота.
   **Правила:** ніколи не зливати локальний `main` у робочу гілку; перед кожним пушем
   `git fetch origin main` і звірка; якщо `git status` каже «ahead N, behind M» —
   ЗУПИНИТИСЬ і розібратися (`git merge-base`, дати комітів), а не тиснути `--force`.

## Пам'ять сесій
також одразу прочитай MEMORY.md у корені репо

Наприкінці кожної сесії мовчки оновлюй MEMORY.md (що зроблено, важливий контекст, відкриті питання) і пушай — без повідомлень користувачу.

## Хто я
- Початківець у Python, потрібні детальні пояснення з прикладами
- Мій основний проєкт — особистий Telegram-бот для себе
- Використовую Claude API (Anthropic)
- Інші сервіси буду підключати в процесі

## Правила роботи зі мною
- Завжди відповідай українською
- Спочатку показуй готовий код, потім пояснення
- Пояснення мають бути детальні, з прикладами — як для початківця
- 🔗 «Дай посилання» / «мені потрібне посилання» = дати КЛІКАБЕЛЬНЕ посилання прямо в чат.
  Повний URL звичайним текстом, щоб можна було натиснути. НЕ в кодовому форматуванні
  (в бектиках воно не клікається), НЕ шлях до файлу, НЕ опис «зайди в панель → Droplets».
  Правило користувачки, 13.08.2026.

## Як оновлювати MEMORY.md

Не чекати команди «онови пам'ять» і не чекати кінця сесії. Записувати одразу при:
- прийнятому технічному рішенні
- значимому коміті/пуші
- виявленому факті/обмеженні, яке знадобиться далі
- незавершеній задачі, яку треба передати наступній сесії

Кожне оновлення MEMORY.md робити тільки в гілці main:

```bash
current=$(git branch --show-current)
git checkout main && git pull origin main
# Edit MEMORY.md (нова секція в кінець)
git add MEMORY.md && git commit -m "memory: <опис>" && git push origin main
git checkout "$current"
```

## Мої проєкти

### Telegram-бот на Claude
Головний файл — bot.py. Залежності — requirements.txt.

**Сервер**
Бот працює на VPS DigitalOcean.
- Шлях: /root/Iryna_Karmazina
- Сервіс: mybot.service (systemd)
- Автодеплой: /root/autodeploy.sh запускається щохвилини, робить git pull і перезапускає сервіс при змінах.

**Git Relay**
Механізм для виконання команд на сервері через GitHub без термінала.
- `cmds/pending.json` — команда для виконання: `{"id":"...", "cmd":"..."}`
- `cmds/result.json` — результат виконання: `{"id":"...", "stdout":"...", "stderr":"...", "returncode":0, "ts":"..."}`
- `cmd_runner.py` — скрипт на сервері, читає pending.json кожні 5 секунд і виконує команди
- Сервіс: cmdrunner.service (systemd)

**Workflow**
Усе через GitHub: коміт у main → за хвилину сервер оновиться.
Руками до сервера не лізьмо.

## Обслуговування MEMORY.md

На початку кожної сесії перевіряй дату `last_memory_cleanup` в MEMORY.md.
Якщо минуло більше 30 днів — нагадай користувачу (одним реченням на початку відповіді):

> "До речі, минув місяць з останнього очищення MEMORY.md — варто прибрати старі записи."

**Як чистити MEMORY.md (раз на місяць) — НІЧОГО НЕ ВИДАЛЯТИ, тільки переносити:**
1. Відкрий MEMORY.md на гілці `main`
2. Секції журналу старші за місяць перенеси ДОСЛІВНО в MEMORY_ARCHIVE.md (у кінець, під заголовком «Перенесено YYYY-MM-DD»)
3. Перед перенесенням: незакриті задачі й важливі факти з цих секцій підніми в «📌 ПОСТІЙНИЙ БЛОК» (мапа гілок / факти / відкриті питання)
4. Онови `last_memory_cleanup`, закомить і запушай у `main`:
   ```bash
   git checkout main && git pull origin main
   # перенеси старі секції в MEMORY_ARCHIVE.md, онови постійний блок і last_memory_cleanup
   git add MEMORY.md MEMORY_ARCHIVE.md && git commit -m "memory: щомісячне перенесення в архів" && git push origin main
   ```

## Користувач
Говорить українською, не програміст — пояснювати простими словами.
